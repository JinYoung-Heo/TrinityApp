<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@drawable/icon_item_background">

    <ImageView
        android:id="@+id/foodIconImageView"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginTop="25dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/foodIconTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="@id/foodIconImageView"
        app:layout_constraintRight_toRightOf="@id/foodIconImageView"
        app:layout_constraintTop_toBottomOf="@id/foodIconImageView"
        android:layout_marginTop="15dp"
        android:textSize="15sp"
        android:gravity="center"
        android:textAlignment="center"
        android:textColor="#666666"
        />

</androidx.constraintlayout.widget.ConstraintLayout>